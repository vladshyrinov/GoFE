{
	"name": "build_home",
	"version": "1.0.0",
	"description": "It's package.json for buid_home site",
	"main": "gulpfile.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
		},
		"author": "Vlad Shyrinov",
		"license": "ISC",
		"devDependencies": {
			"browser-sync": "^2.18.2"
		}
	}
